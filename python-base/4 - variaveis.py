# 📌 Variáveis:
# São espaços na memória que armazenam dados.
# Sintaxe: <nome_da_variavel> = <valor>
# Podem conter letras (maiúsculas ou minúsculas) e underline (_).
# NÃO podem começar com número.
# Evite nomes curtos e sem sentido. Prefira nomes descritivos.
# Para nomes compostos, use snake_case (var_temp) ou camelCase (varTemp).
# Evite nomes muito longos, mas que sejam claros.

# ✔️ Exemplos práticos:
X = 0

x = 7  # Cria a variável x e atribui o valor 7

x        # Exibe o valor de x → 7

x + 3    # Soma 3 ao valor de x → 10

x + x    # Soma x + x → 14

X        # ❌ Erro: Python diferencia maiúsculas e minúsculas. (x ≠ X)

var_temp = 12  # Variável com underline (snake_case)

varTemp = 24   # Variável usando camelCase (menos comum em Python, mas aceitável)

y = x + 8      # Cria variável y com valor de x + 8 → y = 15

y              # Exibe o valor de y → 15

#Para exibir print(value)